# Tinfoil Container Configuration
# For updates: edit this file, commit, run the workflow (e.g., v0.0.2)
cvm-version: 0.10.8
cpus: 2
memory: 8192

containers:
  - name: "demo-safeguard"
    image: "ghcr.io/tinfoilsh/demo-safeguard@sha256:d21d7c54a9d7421bac95184a23d36079e52ae131717feeabc7c4001833c9979a" # v0.0.2
    restart: always
    networks: [web]
    secrets:
      - TINFOIL_API_KEY

networks:
  web:
    egress: open

shim:
  upstream-port: 8080
  paths:
    - /v1/chat/completions
    - /health
  authenticated: true
  authenticated-endpoints:
    - /v1/chat/completions
